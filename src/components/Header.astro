---
import { Image } from 'astro:assets';
// import logoSrc from '../assets/logo.svg';

const navItems = [
  // { name: 'Product', href: '/product' },
  // { name: 'Solutions', href: '/solutions' },
  { name: 'Precios', href: '/precios' },
  // { name: 'About Us', href: '/about-us' },
  // { name: 'Blog', href: '/blog' },
];
---

<header class="sticky top-0 z-50 w-full bg-black/90 backdrop-blur-md border-b border-gray-800" role="banner">
  <div class="container-custom flex items-center h-20">
    <a href="/" class="mr-auto flex items-center transition-transform duration-300 hover:scale-105 focus:scale-105 focus:outline-none focus:ring-2 focus:ring-payflo-purple focus:ring-offset-2 rounded" aria-label="Payflo home">
      <img src="/logo.png" alt="Xendra Pro" class="h-12 w-auto transition-all duration-300 hover:brightness-110" />
    </a>
    
    <!-- Desktop Nav -->
    <nav class="hidden md:flex items-center space-x-8 mr-8" role="navigation" aria-label="Main navigation">
      {navItems.map(item => (
        <a 
          href={item.href} 
          class="text-white hover:text-payflo-purple focus:text-payflo-purple transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-payflo-purple focus:ring-offset-2 rounded px-2 py-1"
        >
          {item.name}
        </a>
      ))}
    </nav>
    
    <div class="flex items-center space-x-4">
      <!-- <a href="/login" class="hidden md:inline-block text-payflo-dark hover:text-payflo-purple focus:text-payflo-purple transition-all duration-300 relative focus:outline-none focus:ring-2 focus:ring-payflo-purple focus:ring-offset-2 rounded px-2 py-1">
        Log In
        <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-payflo-purple transition-all duration-300 group-hover:w-full"></span>
      </a> -->
      <a href="/agendar-demo" class="btn bg-white text-black hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-black">Agendar Demo</a>
      
      <!-- Mobile Menu Button -->
      <button 
        id="menu-toggle" 
        class="md:hidden flex items-center text-white transition-transform duration-300 focus:outline-none focus:ring-2 focus:ring-payflo-purple focus:ring-offset-2 rounded p-1" 
        aria-label="Toggle navigation menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>
  
  <!-- Mobile Menu -->
  <nav id="mobile-menu" class="hidden md:hidden bg-black w-full border-t border-gray-800" role="navigation" aria-label="Mobile navigation">
    <div class="container-custom py-4 space-y-4">
      {navItems.map((item, index) => (
        <a 
          href={item.href} 
          class="block py-2 px-2 text-white hover:text-payflo-purple focus:text-payflo-purple transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-payflo-purple focus:ring-offset-2 rounded"
          style={`transition-delay: ${index * 50}ms;`}
        >
          {item.name}
        </a>
      ))}
      <a href="/login" class="block py-2 px-2 text-white hover:text-payflo-purple focus:text-payflo-purple transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-payflo-purple focus:ring-offset-2 rounded">Log In</a>
    </div>
  </nav>
</header>
